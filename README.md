# Custom theme for bootstrap and material

Need to add 
```
"stylePreprocessorOptions": {
    "includePaths": [
      "./src/app/styles"
    ]
  },
```
into `options` section in angular.json for define path with your scss partials.